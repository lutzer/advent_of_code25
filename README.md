# advent_of_code25

Solutions for [https://adventofcode.com/2025](https://adventofcode.com/2025)

# Usage

* run solutions with `cargo run <part>`
* run solution tests with `cargo test`, show all outputs with `cargo test -- --show-output`
* run specific tests with `cargo test <test_name> -- --show-output`